# Context Snapshot — ARC phases 1-4

- **Task statement:** Build ARC phases 1-4 per the Vault ARC project brief.
- **Desired outcome:** A grounded delivery plan for phase 1 (monorepo/core+profiles migration), phase 2 (adapter interface + Claude adapter), phase 3 (logging framework + `arc logs`), and phase 4 (graceful shutdown + health checks) against the current repo.
- **Known facts / evidence:**
  - Current repo is one TypeScript package with Commander CLI, Ink TUI, and Vitest (`package.json:14-40`, `package.json:73-93`).
  - CLI registration lives in `src/cli.ts:6-220`.
  - Profile/config model lives in `src/config.ts:9-98` and `src/types.ts:1-42`.
  - Tool-specific auth/env behavior lives in `src/auth.ts:168-280`.
  - Tool discovery is static and home-dir based in `src/detect.ts:11-70`.
  - Profile import + Claude-specific `.claude.json` handling live in `src/commands/profile.ts:255-356`.
  - Launch uses direct binary execution with no adapter layer in `src/commands/launch.ts:31-127`.
  - Logging is a single best-effort activity log in `src/log.ts:1-38`.
  - TUI shutdown cleanup is scoped to terminal restoration in `src/tui/render.tsx:7-90`; `doctor`/`status` provide diagnostics in `src/commands/doctor.ts:27-257` and `src/commands/status.ts:40-112`.
  - External brief from `C:\Users\Bailey\SynologyDrive\-Vault-\Axiom-Vault\3. System\Projects\ARC\ARC.md` lists phases 1-4 as monorepo migration, adapter interface + Claude adapter, logging + `arc logs`, graceful shutdown + health checks.
- **Constraints:**
  - Stay within phases 1-4 only.
  - No new dependencies unless justified; reuse existing TypeScript/Vitest/Commander/Ink stack.
  - Preserve current CLI/TUI behavior where possible while creating forward-compatible seams.
- **Unknowns / open questions:**
  - The referenced `DECISIONS.md`, `SPEC.md`, and `docs/supervisor-patterns.md` are not present in this repo snapshot, so planning is grounded in `ARC.md`, `System Audit Action Items.md`, and the current codebase.
  - Exact future monorepo package boundaries beyond phases 1-4 remain flexible.
- **Likely touchpoints:**
  - root: `package.json`, new workspace files, tsconfig/tsup/vitest config
  - core: config, auth, paths, log, shared lifecycle/health
  - adapters: Claude-specific detection/import/launch/auth shape
  - cli/tui: command registration, launch path, doctor/health/logs, dashboard teardown
  - tests: `tests/e2e/cli.test.ts`, `tests/integration/profile.test.ts`, new integration/e2e coverage for logs/health/shutdown
